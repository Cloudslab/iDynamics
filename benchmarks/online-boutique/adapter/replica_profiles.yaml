benchmark: online-boutique
replica_profiles:
  replica1:
    description: Baseline one-pod-per-service profile.
    stateless_replicas: 1
    stateless_services: [adservice, cartservice, checkoutservice, currencyservice, emailservice, frontend, paymentservice, productcatalogservice, recommendationservice, shippingservice]
    single_replica_services: [redis-cart]
    client_services: [loadgenerator]
  replica3:
    description: Three replicas for stateless application services.
    stateless_replicas: 3
    stateless_services: [adservice, cartservice, checkoutservice, currencyservice, emailservice, frontend, paymentservice, productcatalogservice, recommendationservice, shippingservice]
    single_replica_services: [redis-cart]
    client_services: [loadgenerator]
  replica5:
    description: Five replicas for stateless application services.
    stateless_replicas: 5
    stateless_services: [adservice, cartservice, checkoutservice, currencyservice, emailservice, frontend, paymentservice, productcatalogservice, recommendationservice, shippingservice]
    single_replica_services: [redis-cart]
    client_services: [loadgenerator]
