benchmark: sock-shop
replica_profiles:
  replica1:
    description: Baseline singleton compatibility profile.
    application_replicas: 1
    dependency_replicas: 1
  replica3:
    description: Three replicas for REST application services; dependencies stay singleton.
    application_replicas: 3
    dependency_replicas: 1
    application_services: [front-end, catalogue, carts, orders, payment, queue-master, shipping, user]
  replica5:
    description: Five replicas for REST application services; dependencies stay singleton.
    application_replicas: 5
    dependency_replicas: 1
    application_services: [front-end, catalogue, carts, orders, payment, queue-master, shipping, user]
