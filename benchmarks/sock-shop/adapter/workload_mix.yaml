benchmark: sock-shop
generator: locust
default:
  clients: 5
  hatch_rate: 5
  requests: 100
  host: http://front-end.sock-shop.svc.cluster.local
paths:
  - /
  - /catalogue
  - /basket.html
  - /login
claim_boundary: Archived compatibility workload only until ledger-backed.
