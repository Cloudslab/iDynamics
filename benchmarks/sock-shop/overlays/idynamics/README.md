# iDynamics Overlay Notes

No upstream source is stored here. The deploy script fetches the pinned Sock
Shop checkout and applies namespace labels, Istio injection, optional
worker-label placement, and iDynamics collection wrappers at runtime.
