benchmark: train-ticket
generator: compatibility-http-smoke
default:
  users: 1
  duration_seconds: 30
smoke_paths:
  - /
  - /api/v1/users/login
claim_boundary: Smoke only unless replaced by a ledger-backed workload trace.
