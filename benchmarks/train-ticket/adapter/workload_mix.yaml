benchmark: train-ticket
generator: bounded-http-load
default:
  users: 1
  duration_seconds: 30
  concurrency: 2
paths:
  - path: /
    weight: 100
claim_boundary: Smoke/load scaffold only unless replaced by a validated workload trace.
