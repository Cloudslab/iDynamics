benchmark: train-ticket
replica_profiles:
  replica1:
    description: Baseline singleton compatibility profile.
    application_replicas: 1
    dependency_replicas: 1
  replica3:
    description: Scale selected stateless TrainTicket services to three replicas; keep databases singleton.
    application_replicas: 3
    dependency_replicas: 1
  replica5:
    description: Scale selected stateless TrainTicket services to five replicas; keep databases singleton.
    application_replicas: 5
    dependency_replicas: 1
