# Table VIII: Performance and uncertainty of continuous request mixes

This folder contains the curated data and deterministic expected output for `tab:continuous_longmix_robustness`.

Run `bash run.sh --output-dir generated` from this folder to regenerate the data-only artifact. No public full-cluster rerun script is shipped for this item.

Evidence boundary: Same-snapshot replay/model/control-plane policy outputs over generated call-graph traces; not live physical 45-node application latency or universal policy superiority.
