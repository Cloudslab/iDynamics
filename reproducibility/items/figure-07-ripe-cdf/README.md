# Figure 7: Empirical CDF of RIPE Atlas RTT samples with fitted generator

This folder contains the curated data and deterministic expected output for `fig:label_CDF_external_calibration`.

Run `bash run.sh --output-dir generated` from this folder to regenerate the data-only artifact. No public full-cluster rerun script is shipped for this item.

Evidence boundary: Visualizes latency-scale fit only; rare RTT outliers and bandwidth are outside the figure evidence.
