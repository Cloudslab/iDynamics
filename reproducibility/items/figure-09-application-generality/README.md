# Figure 9: Application-generality examples for Online Boutique and CPU-only MoE-style benchmark

This folder contains the curated data and deterministic expected output for `fig:application_generality_longmix`.

Run `bash run.sh --output-dir generated` from this folder to regenerate the data-only artifact. No public full-cluster rerun script is shipped for this item.

Evidence boundary: Shows common adapter and replay schema across an external retail application and a CPU-only MoE-style service graph for K8s, CGA, and HDA; not a universal benchmark-coverage, GPU, token-level routing, or production LLM-serving claim.
