# Table VII: Request-mix generation modes

This folder contains the curated data and deterministic expected output for `tab:request_mix_modes`.

Run `bash run.sh --output-dir generated` from this folder to regenerate the data-only artifact. No public full-cluster rerun script is shipped for this item.

Evidence boundary: Defines workload-mixing semantics and references example generated mode traces; it is not a performance table.
