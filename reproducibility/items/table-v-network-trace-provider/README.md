# Table V: Network trace generation and replay statistics

This folder contains the curated data and deterministic expected output for `tab:network_trace_provider_metrics`.

Run `bash run.sh --output-dir generated` from this folder to regenerate the data-only artifact. No public full-cluster rerun script is shipped for this item.

Evidence boundary: Compares generated and replayed latency/bandwidth matrix statistics; not application latency, scheduler quality, or external WAN validation.
