# Table IV: Accuracy of cross-node network emulation

This folder contains the curated data and deterministic expected output for `tab:ndm_accuracy_summary`.

Run `bash run.sh --output-dir generated` from this folder to regenerate the data-only artifact. No public full-cluster rerun script is shipped for this item.

Evidence boundary: Summarizes 9-worker directed-pair delay and saturated-bandwidth validation; supports emulation accuracy for that controlled matrix only, not production network isolation or WAN realism.
